'' This will create a single integer-type variable.
Sub RoutinesValues()

'' This will create a single integer-type variable.
Dim Array As Array()
Dim Value As Value()
Dim Items As Items()

'' generated from the generated code bellow the generated
Array + Array + Array + Array + Array + Array + Array
Value + Value + Value + Value + Value + Value + Value 
Items + Items + Items + Items + Items + Items + Items

'' produced from the generate code bellow the generated
Array += Array
Value += Value
Items += Items

'' solver generated from the generated code bellow the
Array -= Array
Value -= Value
Items -= Items

'' solver problems generated from the generate code bellow
Array *= Array
Value *= Value
Items *= Items

'' solver problems generated from the generate code bellow
Array /= Array
Value /= Value
Items /= Items

print *, Array /= Value
print *, Array /= Items
print *, Value /= Array
print *, Value /= Items

End